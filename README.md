# 📡 通信系统原理 · 刷题系统
> 期末复习专用 · 离线可用 · 数据本地存储 · 支持GitHub Pages部署
## 🚀 快速开始
### 方式一：本地打开（离线可用）
直接双击 `index.html` 即可在浏览器中打开使用，无需安装任何软件。
### 方式二：GitHub Pages 部署
1. 将整个仓库推送到 GitHub
2. 进入仓库 `Settings → Pages`
3. `Source` 选择 `main` 分支，`/(root)` 目录
4. 点击 `Save`，等待部署完成
5. 访问 `https://你的用户名.github.io/仓库名/`
## 📂 项目结构
```
├── index.html        # 首页（统计看板、搜索、刷题入口）
├── exam.html         # 核心刷题页面
├── wrong.html        # 错题本页面
├── edit.html         # 题库编辑页面
├── setting.html      # 全局设置面板
├── README.md         # 本文件
├── js/
│   ├── question.js   # 通信原理静态题库（69题：32单选+34判断+4计算）
│   ├── storage.js    # localStorage分片存储封装
│   └── common.js     # 通用工具合集
├── css/
│   └── style.css     # 全局样式（浅色/深色双主题、响应式）
└── images/           # 题图存放目录（频谱、波形等）
```
## 📚 题库内容
| 章节 | 单选题 | 判断题 | 计算题 | 合计 |
|------|--------|--------|--------|------|
| 第1-2章小测 | 5 | 8 | 1 | 14 |
| 第3章小测 | 5 | 3 | 0 | 8 |
| 第4-5章小测 | 6 | 9 | 3 | 18 |
| 第6-8章小测 | 16 | 14 | 0 | 30 |
| **合计** | **32** | **34** | **4** | **70** |
## 🎯 功能特性
- **三种题型适配**：单选（点击即答）、判断（对/错按钮）、计算题（多行公式输入，关键词模糊匹配）
- **快捷键支持**：`1-4`单选答题、`Z/X`判断、`Enter`计算提交、`←→`切题、`Space`查看答案
- **深色模式**：完整适配夜间护眼，题图自动降亮度
- **进度可视化**：三色进度条（答对✅/答错❌/未答⬜），独立进度隔离
- **计时器**：单题精确定时，后台切页自动暂停，超时提醒
- **错题本**：多维筛选、排序、批量操作、两种重做模式
- **数据管理**：一键导出/导入JSON备份，支持换设备迁移
- **打印支持**：所有页面可打印，自动隐藏交互控件
## ⌨️ 快捷键速查
| 按键 | 功能 |
|------|------|
| `1` `2` `3` `4` | 单选快速答题 |
| `Z` | 判断选"对" |
| `X` | 判断选"错" |
| `Enter` | 计算题提交答案 |
| `Space` | 显示/隐藏答案 |
| `←` `→` | 上一题/下一题 |
> 快捷键可在设置页全局关闭。
## 🛠️ 题库维护
题库数据源位于 `js/question.js`，每道题的JSON结构：
```javascript
{
  id: 1,                    // 唯一ID
  type: 'single',            // 题型：single/judge/calc
  chapter: '第1-2章小测',    // 章节
  question: '从信号频谱的特点...',  // 题干
  options: [                 // 仅单选题有此字段
    { key: 'A', text: '周期的' },
    { key: 'B', text: '连续的' },
    { key: 'C', text: '离散的' },
    { key: 'D', text: '发散的' }
  ],
  answer: 'C',               // 标准答案
  explanation: '',           // 解析（可选）
  image: ''                  // 配图路径（可选，相对于images/目录）
}
```
添加新题时在 `questionList` 数组中新增对象即可，需清除localStorage中的 `csp_cache` 后刷新页面。
## 💾 数据存储说明
所有数据存储在浏览器localStorage中，分片隔离：
| Key | 内容 |
|-----|------|
| `csp_config` | 全局配置（主题、字体、计时、计分等） |
| `csp_records` | 答题记录（每题的答对/答错次数、历史） |
| `csp_records_archive` | 归档的旧答题记录（可恢复） |
| `csp_errors` | 错题ID列表 |
| `csp_timer` | 总计时、单题用时、超时记录 |
| `csp_cache` | 题库缓存（首次加载后缓存，二次打开更快） |
## 🔧 技术栈
- 纯原生 HTML5 + CSS3 + ES6 JavaScript
- 零外部依赖、零CDN、零框架
- localStorage分片存储
- Flex + Grid 响应式布局
## 📱 兼容性
- PC端：Chrome / Firefox / Edge / Safari
- 移动端：iOS Safari / Android Chrome
- 所有触控区域 ≥ 48×48px
- 计算题输入框适配软键盘
- 低配设备可关闭动画
## 🖨️ 打印使用
按 `Ctrl+P`（Mac: `Cmd+P`）打印当前页面，系统自动隐藏按钮、计时器等交互元素，仅保留题目内容。
## 📄 License
个人学习使用，题库内容版权归原作者所有。